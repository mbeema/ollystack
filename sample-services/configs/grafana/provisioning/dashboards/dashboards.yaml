apiVersion: 1

providers:
  - name: 'OllyStack'
    orgId: 1
    folder: 'OllyStack'
    folderUid: 'ollystack'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards/json
